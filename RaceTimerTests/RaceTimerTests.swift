import Testing
@testable import RaceTimer

struct RaceTimerTests {
    @Test func appLaunches() async throws {
        // Placeholder — confirms the test target links against the app module.
        #expect(true)
    }
}
